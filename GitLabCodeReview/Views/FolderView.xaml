<UserControl x:Class="GitLabCodeReview.Views.FolderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GitLabCodeReview.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <Image Source="/GitLabCodeReview;component/Resources/git-folder.png" Height="16" Width="16" Margin="0,0,3,0"/>
            <TextBlock Text="{Binding DisplayName}" ToolTip="{Binding DisplayName}"/>
        </StackPanel>
    </Grid>
</UserControl>
